// SPECS.md "ANCIENT RUINS PROCESS" — Tables 1-3 (count), 1-4 (type), 1-5 (menace),
// 1-6 (age), 1-7 (original purpose), 1-8 (reason for ruin). PDF pages 12, 12, 13, 18, 18, 19.

/**
 * @param {ReturnType<typeof import("./region.mjs").createRegion>} region
 * @returns {Promise<{ ruins: object[] }>}
 */
export async function generateAncientRuins(region) {
    throw new Error("generateAncientRuins not yet implemented — see SPECS.md ANCIENT RUINS PROCESS / Tables 1-3..1-8");
}
