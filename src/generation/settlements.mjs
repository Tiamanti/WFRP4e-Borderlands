// SPECS.md "COMMUNITIES SUMMARY" — generated per principality and once for uncontrolled areas.
// Town presence (GM call), Table 3-1 (villages), 1d10 homesteads, Table 3-2 (+ sub-tables) for
// special features per settlement. PDF pages 46, 47, 48-51.

/**
 * @param {ReturnType<typeof import("./region.mjs").createRegion>} region
 * @returns {Promise<{ settlements: object[] }>}
 */
export async function generateSettlements(region) {
    throw new Error("generateSettlements not yet implemented — see SPECS.md COMMUNITIES SUMMARY / Tables 3-1, 3-2");
}
