// SPECS.md "RELATIONS GENERATION SUMMARY" — split princes into groups, then per group:
// Table 2-12 (nature), 2-13 (duration), and the matching cause table from 2-14..2-22
// (chosen by the nature rolled on 2-12). PDF pages 37, 37, 37-43.

/**
 * @param {ReturnType<typeof import("./region.mjs").createRegion>} region
 * @returns {Promise<{ relationships: object[] }>}
 */
export async function generateRelationships(region) {
    throw new Error("generateRelationships not yet implemented — see SPECS.md RELATIONS GENERATION SUMMARY / Tables 2-12..2-22");
}
